<?php

// guessing game 

# generate random number(1-100)


#ask the user to guess number 

#if(number == random number ) - game over 

#if(number > random number )- retry, too high

# if(number > random number ) - retry ,too low